---
kind: Service
apiVersion: v1
metadata:
  name: vegetaserver
spec:
  selector:
    name: vegetaserver
    app: vegetaserver
    tier: api
  ports:
    - name: api
      protocol: TCP
      port: 8080

---
kind: Deployment
apiVersion: apps/v1beta1
metadata:
  name: vegetaserver
  labels:
      name: vegetaserver
      app: vegetaserver
      tier: api
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: vegetaserver
        app: vegetaserver
        tier: api
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: default-pool
      containers:
        - name: vegetaserver
          imagePullPolicy: Always
          image: gronnbeck/vegetaserver
          env:
          - name: TARGET_URL
            value: http://helloworldserver.my-namespace.svc.cluster.local
          ports:
            - name: api
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 20
            timeoutSeconds: 2
